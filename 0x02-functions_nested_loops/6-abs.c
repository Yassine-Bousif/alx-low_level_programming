#include "main.h"
/**
 * _abs - absolute value function
 * compute the absolute value of an integer
 * @n: yje targeted num
 * Return: -n or n
 *
 */

int _abs(int n)
{
	if (n < 0)
		{
		return (-n);
		}
	else
		{
		return (n);
		}
}
