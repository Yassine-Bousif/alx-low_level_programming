#include"main.h"

/**
 * _isdigit - a func that checks for a digit (0 through 9).
 * @c: The digit to be checked
 * Return: 1 if c is a digit, 0 if not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
